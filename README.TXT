This application needs a dataset of virus.

That dataset's size is about 140mb.
So that a sample of that file is located in the project location and compressed sql file is there if needed.

If the full set of dataset need please extraxt the vx.7z and replace the vx.sql file in the project before run the application.

If you run buid jar file please copy the vx.sql file if willing to create database.