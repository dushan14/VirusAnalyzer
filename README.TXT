This application needs a dataset of virus.

To run this you need to run mysql local server on your machine.

That dataset is about 140mb.
So, a sample of that file is located in the project location and compressed sql file is also there if needed.

If the full dataset is needed please extraxt the vx.7z and replace the vx.sql file in the project before run the application.

If you run buid jar file please copy the vx.sql file if willing to create database.
